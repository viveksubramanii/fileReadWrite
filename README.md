echo "# fileReadWrite" >> README.md
git init
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/viveksubramanii/fileReadWrite.git
git push -u origin master

use format as:

node index input.txt output.txt
